import { Rnd } from "react-rnd"

export const Timer = () => {
    return(
        <Rnd bounds='window' enableResizing={false}>
            <div>
                
            </div>
        </Rnd>
    )
}